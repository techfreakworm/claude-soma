"""Phase-1 lead-parallelism package.

This package is INERT unless the module is explicitly imported and
init_db() is called.  Nothing here auto-runs on import, and nothing in
spawner.py or server.py references this package -- flag-off leads are
byte-identical to today.
"""
