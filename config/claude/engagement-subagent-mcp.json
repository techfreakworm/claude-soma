{
  "_comment": "Minimal MCP-config for the FI-ENGAGEMENT-FRESH-DRIP hourly browse+draft subagent. After FI-ENGAGEMENT-FRESH-DRIP-AUTH (2026-06-05): NO playwright MCPs. The MCP-driven playwright opens a fresh unauthenticated browser context in subagent/dispatched contexts (see engagement-post-x.js header for the same finding on posting). The subagent instead drives /opt/claude-soma/scripts/engagement-browse-{x,linkedin}.js via Bash — those load the same storageState files the proven post scripts use and see the real authenticated feeds. Only hermes-api remains, for queue writes via send_tg_reply-adjacent tooling.",
  "mcpServers": {
    "hermes-api": {
      "type": "stdio",
      "command": "/opt/claude-soma/.venv/bin/python",
      "args": ["-m", "claude_soma.mcp_servers.hermes_api.server"],
      "env": {
        "HERMES_API_SOCKET": "/tmp/claude-soma-api.sock",
        "HERMES_ACTIVITY_LOG": "/home/ubuntu/.claude-soma/activity.jsonl"
      },
      "alwaysLoad": true
    }
  }
}
