# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.eggs/
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
.turbo/
.pnpm-store/
out/
*.tsbuildinfo

# Build artifacts
dist/
build/
*.log

# Local config and secrets — NEVER commit
.env
.env.*
!.env.example
secrets.env
*.pem
*.key
*.token
config.local.*
meta-tokens.env
social.env
.claude/settings.local.json
.claude.local.md

# Google service-account credentials (no code consumer in this repo;
# used only by one-off out-of-band agent tasks — never commit)
google/

# Runtime data
*.sqlite
*.sqlite-journal
*.db
registry.sqlite
usage.sqlite
activity.jsonl
/var/log/hermes-claude/

# OAuth tokens — extra explicit
CLAUDE_CODE_OAUTH_TOKEN*
oat-*

# Voice and tmp media
/tmp/voice_*
/tmp/tts_*

# IDE
.vscode/
.idea/
*.swp
*~

# Editor backup
*.bak

# Project-lead working dirs (created at runtime per spawn)
/home/ubuntu/projects/

# Hermes-Claude runtime state
.hermes-claude/
archive/

# Sub-repositories cloned for reference (do not include in our git)
/llm/hermes-agent/
